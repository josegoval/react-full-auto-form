import React, { ReactElement } from 'react'
import { Fields } from '../../../../core/types/propTypes/fields'

type FieldsProps = {
  fields: Fields
}

export default function Inputs({ fields }: FieldsProps): ReactElement {
  return <div />
}
